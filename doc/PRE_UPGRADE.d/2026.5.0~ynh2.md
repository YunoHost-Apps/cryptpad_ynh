* Move data dir into /home/yunohost.app/cryptpad
* Move log dir into /var/log/cryptpad
* Manage admin key from config panel. Note this will remove all current admin key in the configuration, so it should be backup if needed.
